commflags = -O2 -I.

warns += -Wall -Wextra -pedantic -Werror

includes += -isystem external

cxx = clang -std=c++14 $(commflags) $(warns) $(includes)

: foreach core/*.cpp |> ^ cxx %f -o %o ^ $(cxx) -c %f -o %o |> build/%B.o {obj}
